
    export function getRandomInt(max:number) : number {
        return Math.floor(Math.random() * max);
    }
    export interface ElementPointData {
        x: number,
        y: number,
        height: number,
        width: number
    }